# GUr03 / Dr8p5 布局分析报告

目标节点：`GUr03` CCTV List，`Dr8p5` DeleteConfirmPopup。

## GUr03 Visual Hierarchy

- 根节点 `GUr03`：1024 x 600，深色渐变背景。
- `NqMgu` Header：x=60 y=24，返回按钮 48 x 48，标题 `CCTV 설정`，Inter 32/500。
- 描述文本：x=60 y=86，高 28，`등록된 카메라 목록을 관리합니다`。
- 列表容器：x=60 y=134，宽 904，高 366。
  - 单条列表项：宽 904，高 86.5，radius 16，fill `#ffffff0d`，stroke `#ffffff1a`。
  - 左侧 CCTV 图标盒：44 x 44，fill `#51a2ff33`。
  - 标题：`CCTV1` / `CCTV2`，Inter 18/500，白色。
  - 副标题：`IP:80 • account`，Inter 13/500，`#ffffff80`。
  - 删除按钮 `CZq0T`：36 x 36，fill `#ff64671a`，stroke `#ff646733`。
  - 右箭头：24 x 24，`#ffffff66`。
- 添加按钮 `zZElu`：x=60 y=520，宽 904，高 56，radius 14，fill `#51a2ff1f`，stroke `#51a2ff66`，文本 `새 카메라 추가`。

## Dr8p5 Visual Hierarchy

- 根节点 `Dr8p5`：1024 x 600，背景为列表页叠加 `#000000b2` 遮罩。
- 弹窗容器：x=292 y=177.5，宽 440，高 245，radius 20，深色渐变，stroke `#ffffff1a`。
- 顶部区域：高 77，左侧红色删除图标 36 x 36，标题 `카메라 삭제`。
- 内容：
  - 主文案：`CCTV1을(를) 삭제하시겠습니까?`，Inter 20/500，蓝色 `#51a2ff`。
  - 副文案：`삭제된 카메라 정보는 복구할 수 없습니다.`，Inter 16，`#ffffff73`。
- 底部按钮：
  - 取消：x=24 y=177，189 x 48，fill `#ffffff14`。
  - 删除：x=225 y=177，189 x 48，fill `#ff646726`，text `#ff6467`。

## 交互要求

- `MpUw5` 进入 `GUr03`。
- 列表成员点击进入 `foYV2`，显示该 CCTV 信息。
- `CZq0T` 打开 `Dr8p5` 删除确认弹窗。
- `zZElu` 进入新增页面 `UGcOs`。
